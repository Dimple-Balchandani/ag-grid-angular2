import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import {By} from '@angular/platform-browser';
import { AgGridModule} from 'ag-grid-angular/main';
import { NameCellChangeComponent } from './name-cell-change.component';
import { DebugElement } from '@angular/core';

describe('NameCellChangeComponent', () => {
  // let component: NameCellChangeComponent;
  // let fixture: ComponentFixture<NameCellChangeComponent>;
  // let de:      DebugElement;
  // let el:      HTMLElement;
  //
  // beforeEach(async(() => {
  //     TestBed.configureTestingModule({
  //         declarations: [ NameCellChangeComponent ],
  //         imports: [
  //               AgGridModule.withComponents([NameCellChangeComponent])
  //         ]
  //     })
  //     .compileComponents();
  // }));
  //
  // beforeEach(() => {
  //     fixture = TestBed.createComponent(NameCellChangeComponent);
  //     component = fixture.componentInstance;
  //     const params = {'value': 'Adidas'};
  //     component['params'] = params;
  //     fixture.detectChanges();
  // });
  //
  // it('should create', () => {
  //     expect(component).toBeTruthy();
  // });
  //
  // it('check param value', () => {
  //     de = fixture.debugElement.query(By.css('span'));
  //     el = de.nativeElement.innerText;
  //
  //     expect(component.params['value']).toEqual(el);
  // });

});
